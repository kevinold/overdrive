# mise pins. Agent configs launch codebase-memory-mcp as `mise exec github:DeusData/codebase-memory-mcp@<pin> -- codebase-memory-mcp`,
# so it resolves from any project, not only this clone; keep the pin in sync (tests/mcp-probe.test.js).
# See docs/harness-inventory.md.
[tools]
"github:DeusData/codebase-memory-mcp" = "0.11.0"
act = "latest"
actionlint = "latest"
gh = "latest"
jq = "latest"
aws = "latest"
