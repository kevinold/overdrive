---
description: overdrive harness context contract
alwaysApply: true
---

Read `AGENTS.md` at the repo root for the full context contract, model gears, and the
`cross_model_peer: codex` egress disclosure.

- MCP servers: `context7` and `codebase-memory-mcp` (see `.mcp.json`).
- Cursor gets AGENTS.md + the two MCP servers; full matrix in `docs/capability-matrix.md`.
- Compounding loop: durable learnings live in `docs/solutions/`.
