# overdrive — Gemini shim

Read `AGENTS.md` for the full context contract, model gears, and egress disclosure.

- Use the MCP servers wired in `.gemini/settings.json` (`context7`, `codebase-memory-mcp`).
- Use the skills/conventions described in `AGENTS.md`.
- Gemini gets AGENTS.md + the two MCP servers; full matrix in `docs/capability-matrix.md`.
